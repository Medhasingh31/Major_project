"""Lightweight road extraction and graph generation package."""

__version__ = "0.1.0"

# ---------------------------------------------------------------------------
# Geometry + Topology pipeline (separate from the U-Net segmentation pipeline)
# Usage:
#   from road_extractor.geometry  import extract_geometry
#   from road_extractor.topology  import build_topology
#   from road_extractor.confidence import score_topology
# ---------------------------------------------------------------------------
